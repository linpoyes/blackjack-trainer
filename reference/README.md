# 原始 artifact 存檔

`original-artifact.html` 是 claude.ai 對話「澳門21點勝率策略與下注技巧」裡
Blackjack trainer artifact 的**未修改原檔**，2026-08-18 由使用者下載。

保留原因：這是唯一的原始副本，git 第一個 commit 已經是修改後的版本。

**不要直接開這個檔來用** —— 它用 `window.storage`（Artifact 專用 API），
在瀏覽器環境不存在且被 try/catch 吞掉，重整頁面所有練習記錄會消失。
可用的版本是上層的 `index.html`。
